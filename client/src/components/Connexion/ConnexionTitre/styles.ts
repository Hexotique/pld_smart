import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    composant:{
        flex:1,
        alignItems:'center'
    },

    titre:{
        fontSize:50,
        fontWeight:'bold',
        color:'white',
        marginBottom:10,
        marginTop:10,
    }

});

export default styles;